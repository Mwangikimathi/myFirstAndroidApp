<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.muniu.myapplication">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".dashBoardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UONActivity"
            android:label="@string/title_activity_uon"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".JkuatActivity"
            android:label="@string/title_activity_jkuat"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MoiActivity"
            android:label="@string/title_activity_moi"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".webPageAccess"></activity>
    </application>

</manifest>